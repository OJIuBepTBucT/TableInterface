import Foundation
import UIKit

struct Images {
  static let injection = UIImage(named: "injection")!
  static let clock = UIImage(named: "clock")!
  static let placeholder = UIImage(named: "placeholder")!
  static let user = UIImage(named: "user")!
  static let phone = UIImage(named: "phone")!
}
